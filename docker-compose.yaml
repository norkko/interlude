version: '3.7'
services:
  eureka:
    container_name: eureka
    build: ./eureka
    ports:
      - 8761:8761

  service:
    container_name: service
    build: ./service
    ports:
      - 8080:8080
    links:
      - eureka
    depends_on:
      - eureka